global:
  scrape_interval: 5s # Fast updates for real-time monitoring

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['host.containers.internal:8889'] # The port where your OTel Collector exports metrics